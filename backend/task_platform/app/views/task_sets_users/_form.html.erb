<%= form_with(model: task_sets_user) do |form| %>
  <% if task_sets_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_sets_user.errors.count, "error") %> prohibited this task_sets_user from being saved:</h2>

      <ul>
        <% task_sets_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_set_id %>
    <%= form.collection_select :task_set_id, Task.all, :id, :representable_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.all, :id, :representable_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
