<%= form_with(model: task_sets_user) do |form| %>
  <% if task_sets_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_sets_user.errors.count, "error") %> prohibited this task_sets_user from being saved:</h2>

      <ul>
        <% task_sets_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_set_id %>
    <%= form.text_field :task_set_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
